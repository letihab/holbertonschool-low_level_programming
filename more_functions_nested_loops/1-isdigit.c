# include "main.h"

/**
 *_isdigit _ function that checks for a digit
 *@c: check if c is a digit
 *Return 0
 */
int _isdigit(int c)
{
    if (c >= 48 && c <= 57)
	{
	return (1);
	}
	else
	return (0);
}
