#include "main.h"

/**
 *Print_numbers - print the numbers, from 0 to 9
 *@void : parameter of function
 */
void print_numbers(void)
{
	int i;
 
	for (i = 0; i <= 9; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
